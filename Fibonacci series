#include<stdio.h>
int main()
{
    int a=0,b=1,n;
    scanf("%d",&n);
    printf("%d, %d, ",a,b);
    int nextterm;
    for(int i=2;i<n;i++){
    nextterm=a+b;
    a=b;
    b=nextterm;
    printf("%d, ",nextterm);
} return 0;
}
